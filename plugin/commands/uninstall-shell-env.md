---
description: Remove the macOS LaunchAgent installed by /install-shell-env and unset the forwarded launchd env vars.
argument-hint: "[--keep-vars] [--dry-run]"
allowed-tools: ["Bash"]
---

Run: `npx -y curato uninstall-shell-env $ARGUMENTS 2>&1`

Show the output as-is.
